# دليل نشر MOKHTAR GYM على Vercel — خطوة بخطوة

هذا الدليل يشرح كيف تنقل تطبيق MOKHTAR GYM كاملاً إلى استضافة دائمة ومجانية على Vercel، مع قاعدة بيانات Turso سحابية.

**ملاحظة مهمة قبل البدء**: المشروع معدّل وجاهز 100% للنشر — كل ما عليك هو إنشاء 3 حسابات مجانية وضبط قيم البيئة. لا يحتاج أي تعديل على الكود.

---

## المتطلبات (كلها مجانية)

| الحساب | الرابط | الغرض |
|---|---|---|
| GitHub | github.com | استضافة الكود (مستودع **خاص**) |
| Turso | turso.tech | قاعدة البيانات السحابية |
| Vercel | vercel.com | استضافة التطبيق |
| Brevo | عندك حساب أصلاً | إرسال البريد عبر API |

---

## الخطوة 1: رفع المشروع إلى GitHub

1. سجّل دخول إلى **github.com** (أنشئ حساباً إن لم يكن عندك).
2. اضغط زر **+** أعلى اليمين → **New repository**.
3. الاسم: `mokhtar-gym` — اختر **Private** (مهم جداً! الكود خاص) → **Create repository**.
4. اضغط على رابط **uploading an existing file** في الصفحة التي تظهر.
5. اسحب كل محتويات مجلد المشروع (بعد فك الضغط) إلى صفحة الرفع → **Commit changes**.

> ⚠️ لا ترفع مجلد `node_modules` ولا ملف `.env` — غير موجودين أصلاً في الأرشيف.

---

## الخطوة 2: إنشاء قاعدة البيانات على Turso

1. ادخل إلى **turso.tech** → سجّل بحساب GitHub (زر Continue with GitHub).
2. من لوحة التحكم اضغط **Create Database**.
3. الاسم: `mokhtar-gym` → المنطقة (Location): اخير **Europe (Frankfurt أو أي مدينة أوروبية)** → **Create**.
4. بعد الإنشاء، اضغط على اسم القاعدة → ستظهر **Database URL** مثل:
   ```
   libsql://mokhtar-gym-yourname.turso.io
   ```
   **انسخها واحتفظ بها** — ستحتاجها في الخطوة 4.
5. من نفس الصفحة اضغط **Generate Auth Token** (أو Settings → Database Authentication) → انسخ التوكن الطويل (يبدأ بـ `eyJ...`).

> الخطة المجانية: 500 قاعدة + 9GB تخزين + مليار قراءة — أكثر من كافي لصالة كاملة.

---

## الخطوة 3: مفتاح Brevo API

1. ادخل إلى **app.brevo.com** → **SMTP & API** (من القائمة الجانبية).
2. تبويب **API Keys** → **Generate a new API key v3**.
3. الاسم: `mokhtar-vercel` → الصلاحيات: **Transactional email** → **Generate**.
4. انسخ المفتاح (يبدأ بـ `xkeysib-`) — **يظهر مرة واحدة فقط**.

> ملاحظة: هذا مختلف عن مفتاح SMTP الذي عندك حالياً (الذي يبدأ بـ `xsmtpsib-`).

---

## الخطوة 4: نشر التطبيق على Vercel

1. ادخل إلى **vercel.com** → **Continue with GitHub** (بنفس حساب GitHub).
2. اضغط **Add New Project** → اختر مستودع `mokhtar-gym` → **Import**.
3. قبل الضغط على Deploy، اذهب إلى قسم **Environment Variables** وأضف هذه المتغيرات الأربعة:

| Name | Value |
|---|---|
| `DATABASE_URL` | رابط Turso من الخطوة 2 (يبدأ بـ `libsql://`) |
| `DATABASE_AUTH_TOKEN` | التوكن الطويل من الخطوة 2 (يبدأ بـ `eyJ...`) |
| `AUTH_SECRET` | `888vYCCbmEzm8GyOJGkrldw4kUv2dMdGOrREGd17vmZqe0NA` |
| `BREVO_API_KEY` | مفتاح Brevo من الخطوة 3 (يبدأ بـ `xkeysib-`) |

4. اضغط **Deploy** وانتظر 3-5 دقائق حتى ينتهي البناء.
5. عند النجاح ستحصل على رابط دائم مثل: **`mokhtar-gym.vercel.app`** 🎉

> بعد أول نشر، أي تعديل على الكود في GitHub يُنشر تلقائياً.

---

## الخطوة 5: نقل بياناتك إلى قاعدة Turso

بياناتك الحالية (الأعضاء، الاشتراكات، المدفوعات...) موجودة في ملف `db/custom.db` داخل نسختك المحلية من المشروع. لنقلها:

**الطريقة الأسهل**: أرسل لي الرابطين من الخطوة 2 (Database URL + Auth Token) وسأنفذ النقل وأتحقق من كل جدول. (سكربت النقل موجود في `scripts/migrate-to-turso.ts`)

**أو من جهازك** (إذا منصّب Bun):
```bash
TURSO_DATABASE_URL="libsql://..." TURSO_AUTH_TOKEN="eyJ..." bun scripts/migrate-to-turso.ts
```

---

## الخطوة 6: تحويل تطبيق الأندرويد للرابط الجديد

على الهاتف: **اضغط ضغطاً مطوّلاً على أيقونة MOKHTAR GYM** → اختر **"رابط السيرفر"** → امسح القديم واكتب:
```
mokhtar-gym.vercel.app
```
اضغط اتصال — انتهى! التطبيق الآن يعمل على الرابط الدائم.

---

## حل المشاكل الشائعة

| المشكلة | الحل |
|---|---|
| البناء فشل على Vercel | تأكد أن المجلد رُفع كاملاً (خصوصاً `package.json` و`bun.lock`) |
| صفحة الخطأ بعد النشر | راجع Environment Variables — الخطة المجانية تظهر التفاصيل في تبويب Logs |
| البريد لا يصل | تأكد من `BREVO_API_KEY` + توثيق المرسل في Brevo (Senders) |
| دخول الأدمن لا يعمل | تأكد أن الخطوة 5 (نقل البيانات) نُفذت — القاعدة الفارغة لا تحتوي حسابات |

## ملاحظات تقنية سريعة

- كلمة مرور الأدمن وحسابات الأعضاء تبقى كما هي — تنتقل مع البيانات.
- الصور محفوظة داخل القاعدة نفسها، تنتقل تلقائياً مع النقل.
- الخطة المجانية في Vercel مخصصة رسمياً للمشاريع الشخصية — إذا كبر المشروع تجارياً ترقّ إلى Pro بـ $20/شهر.
- نسخة احتياطية: قاعدة Turso → زر Export في لوحة التحكم ينزّل ملف SQL.
